# Copy to .env and fill in values. Do not commit .env.

# Postgres (used by all app services)
POSTGRES_PASSWORD=portfolio

# Kite Connect (kite-portfolio, stocks-refdata, kite-market-data)
KITE_API_KEY=your_kite_api_key
KITE_API_SECRET=your_kite_api_secret

# Telegram (telegram service; kite-portfolio uses CHAT_ID for outbound)
TELEGRAM_BOT_TOKEN=your_telegram_bot_token
CHAT_ID=your_telegram_chat_id

# Optional
# REFDATA_RUN_ON_START=1          # Run stocks-refdata job once on startup
# MARKET_DATA_INTERVAL=60         # Seconds between LTP fetches (default 60)
